线程属性
1.编号
  线程id从1开始不是0，子线程不是从2开始，因为JVM会启动一些必要的线程
2.名称
3.守护线程
  true-守护线程 false-用户线程
  守护线程位用户线程提供服务
  线程类型默认继承自父线程
  守护线程被JVM启动
  守护线程不影响JVM退出，JVM只检查有没有用户线程
4.优先级
  告诉调度器那些线程多运行一点，那些少运行一点
  Java中有10个级别，默认5
  程序设计不应依赖优先级（优先级依赖操作系统）：
    不同操作系统优先级不同
    优先级会被操作系统改变